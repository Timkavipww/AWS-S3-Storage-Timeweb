# IDE system files and directories
.vscode/
*.sln
*.user

# .NET build files
bin/
obj/

# Configuration files (if they shouldn't be committed)
appsettings.json
appsettings.Development.json

# HTTP request files (if using REST Client in VS Code)
*.http

# Exclude images and text files (if they shouldn't be in the repository)
*.jpg
*.txt
.env
